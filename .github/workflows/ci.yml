name: CloneTest
on:
    push:
jobs:
    Build:
        runs-on: ubuntu-latest
        steps:
        - name: JDKInstall
          uses: joschi/setup-jdk@v2.3.0
          with:
            java-version: 21
        - name: Build
          run: ./gradlew check
