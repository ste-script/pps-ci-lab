name: CloneTest
on:
    push:
jobs:
    Build:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v2
        - uses: actions/setup-java@v4
          with: 
            distribution: 'adopt'
            java-version: '21'
        - name: Build
          run: ./gradlew check
